---
import Base from './Base.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Base title={title}>
  <style slot="head">
    @media print {
      body {
        print-color-adjust: exact !important;
      }
    }
  </style>
  <slot />
</Base>
